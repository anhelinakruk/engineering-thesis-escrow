services:
  surrealdb:
    container_name: escrow-surrealdb
    hostname: surrealdb
    build:
      context: .
      dockerfile: ./surrealdb.Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - surrealdb-data:/data

volumes:
  surrealdb-data:
