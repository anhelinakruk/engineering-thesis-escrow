# SurrealDB root credentials (dev only — set real values in .env)
SURREAL_USER=root
SURREAL_PASS=root

# Namespace / database the fixtures define (see fixtures/setup.surql)
SURREAL_NS=escrow
SURREAL_DB=wallet
